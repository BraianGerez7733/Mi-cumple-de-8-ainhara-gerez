# Invitacion de cumpleanos de Ainhara Gerez

Invitacion interactiva hecha con Vue 3, Vite y Tailwind CSS.

## Ejecutar

```bash
npm install
npm run dev
```

## Persistencia

El proyecto usa `localStorage` por defecto y puede conectarse a Supabase con las variables del archivo `.env.example`.
